<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

             xmlns:viewModels="clr-namespace:PFAssistant.Core.ViewModels;assembly=PFAssistant.Core"
             x:Class="PFAssistant.Core.View.MenuPage"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:MenuViewModel}"
             Title="{Binding Title}">

    <Label Text="{Binding Title}" VerticalOptions="Center" HorizontalOptions="Center" />
    <ListView ItemsSource="{Binding Items}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell

                    Text="{Binding Title}" Detail="{Binding Description}" Command="{Binding OnClick}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>